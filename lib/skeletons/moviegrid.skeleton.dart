import 'package:flutter/material.dart';

class Movie_Grid_Skeleton extends StatelessWidget {
  const Movie_Grid_Skeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Skeleton for movie grid');
  }
}
